{
  "name": "@koi/editor",
  "version": "0.0.1",
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npx electron-builder --config packages/editor/electron-builder.config.js"
        },
        "dependsOn": [
          "@koi/electron-main:build",
          "@koi/web-ui:build"
        ],
        "parallel": true
      }
    }
  }
}
